'use client';
import { useState } from 'react';
import { Send, CheckCircle2, ShieldCheck } from 'lucide-react';
export default function Contact(){const [sent,setSent]=useState(false);const [error,setError]=useState('');function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const name=String(f.get('name')||'').trim();const mobile=String(f.get('mobile')||'').trim();const email=String(f.get('email')||'').trim();if(name.length<2){setError('Please enter your full name.');return}if(!/^[0-9]{10}$/.test(mobile)){setError('Please enter a valid 10-digit mobile number.');return}if(email && !/^\S+@\S+\.\S+$/.test(email)){setError('Please enter a valid email address.');return}setError('');setSent(true);form.reset()}return <section id="contact" className="section contact"><div className="container contact-card"><div className="contact-copy"><p className="eyebrow">PRIVATE ENQUIRY</p><h2>Let’s make your next address <em>extraordinary.</em></h2><p>Request project details, floor plans or a guided site visit. Our team will get back to you shortly.</p><div className="contact-points"><span>+91 9090 701 701</span><span>info@goyalinternationalcity.com</span></div><div className="privacy-note"><ShieldCheck size={16}/> Your details stay private and are used only for this enquiry.</div></div><form onSubmit={submit} noValidate>{sent?<div className="success"><CheckCircle2 size={42}/><h3>Thank you.</h3><p>Your enquiry has been recorded. We’ll be in touch.</p><button type="button" className="btn ghost" onClick={()=>setSent(false)}>Send another enquiry</button></div>:<><div className="form-grid"><label>Name<input name="name" placeholder="Your full name" autoComplete="name" required/></label><label>Mobile<input name="mobile" inputMode="numeric" maxLength={10} placeholder="10-digit number" autoComplete="tel" required/></label></div><label>Email<input name="email" type="email" placeholder="you@example.com" autoComplete="email"/></label><label>Message<textarea name="message" placeholder="Tell us what you’d like to know…" rows={4}/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="btn primary form-submit" type="submit">Send Inquiry <Send size={17}/></button></>}</form></div></section>}
