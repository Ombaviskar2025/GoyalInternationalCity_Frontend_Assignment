'use client';
import { useEffect, useState } from 'react';
import { X, Maximize2 } from 'lucide-react';
const images=['https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?auto=format&fit=crop&w=1200&q=85','https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&w=1000&q=85','https://images.unsplash.com/photo-1600566753086-00f18fb6b3ea?auto=format&fit=crop&w=1000&q=85','https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=1000&q=85','https://images.unsplash.com/photo-1600607688969-a5bfcd646154?auto=format&fit=crop&w=1000&q=85'];
export default function Gallery(){const [selected,setSelected]=useState<string|null>(null);useEffect(()=>{if(!selected)return;const onKey=(e:KeyboardEvent)=>e.key==='Escape'&&setSelected(null);window.addEventListener('keydown',onKey);document.body.style.overflow='hidden';return()=>{window.removeEventListener('keydown',onKey);document.body.style.overflow=''}},[selected]);return <section id="gallery" className="section gallery"><div className="container"><div className="section-head"><div><p className="eyebrow">PROJECT GALLERY</p><h2>A glimpse into <em>elegance.</em></h2></div><p>Architecture, landscape and lifestyle come together in a visual story of the township.</p></div><div className="gallery-grid">{images.map((src,i)=><button key={src} className={i===0?'gallery-item large':'gallery-item'} onClick={()=>setSelected(src)} style={{backgroundImage:`url(${src})`}} aria-label={`Open gallery image ${i+1}`}><span><Maximize2 size={17}/></span></button>)}</div></div>{selected&&<div className="lightbox" role="dialog" aria-modal="true" aria-label="Gallery preview" onClick={()=>setSelected(null)}><button aria-label="Close gallery" onClick={(e)=>{e.stopPropagation();setSelected(null)}}><X/></button><img src={selected} alt="Goyal International City gallery preview" onClick={e=>e.stopPropagation()}/></div>}</section>}
